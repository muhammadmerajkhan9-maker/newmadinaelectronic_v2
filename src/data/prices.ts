// Centralized price override list for New Madina Electronic
// Prices can be overridden centrally here without touching the catalogue entries.
export const priceList: Record<string, { price: number; was?: number | null }> = {};
